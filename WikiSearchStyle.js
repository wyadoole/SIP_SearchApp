import { makeStyles } from "@material-ui/core/styles";

const wikiSearchStyles= makeStyles(theme => ({
    container: {
        display: "flex",
        flexDirection: 'column',
        maxWidth: '50vw',
        margin: 'auto'
    },
    search: {

    },
    button: {

    }
}))

export default wikiSearchStyles
